version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - npm install -g npm@latest
            - cd backend
            - npm install
        build:
          commands:
            - npm run build
            - cd ..
      artifacts:
        baseDirectory: dist
        files:
          - "**/*"
      cache:
        paths:
          - node_modules/**/*
    appRoot: .
